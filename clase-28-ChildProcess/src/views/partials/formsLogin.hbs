<!-- Formulario -->
<div class="jumbotron">
    <h1 style="color:blue;">Loguin datos</h1>
    <br>
    {{!-- <h3>Por favor ingrese los siguientes datos para ingresar</h3>
    <h3>usuario: pepe, password: pepepass</h3> --}}
    <form action="/login" id="formLog" method="POST">
        <div class="form-group">
            <label for="username"><b>Mail</b></label>
            <input id="username" class="form-control" type="text" name="username" onchange="">
        </div>
        <div class="form-group">
            <label for="password"><b>Password</b></label>
            <input id="password" class="form-control" type="text" name="password" onchange="">
        </div>
        <button type="submit" class="btn btn-success mt-3 mb-" id="log">Login</button>

    </form>
    <form action="/registro" method="GET">
        <button class="btn btn-success mt-3 mb-5">Registrarse</button>
    </form>
    {{!-- <script>

        const form = document.getElementById('formLog');
        
        form.addEventListener('submit',async (event)=>{
            event.stopPropagation();
            event.preventDefault();
            console.log('ejecutando')
            
            let datos = {username: form.username.value, password:form.password.value}
            await fetch("/prueba", {
            body: JSON.stringify(datos),
            headers: {
                "Content-Type": "application/json",
                // "Content-Type": "multipart/form-data",
            },
            method: "POST",
        })
        .then((res)=>{console.log(res.value)
        })
        .catch(err => console.log(err))
        .finally(console.log('se realizo'))
        });
        
    </script> --}}
</div><!-- Final Formulario -->
    
